version: "3.8"

services:
  ultrastream:
    container_name: ultrastream
    build:
      context: .
      dockerfile: Dockerfile

    ports:
      - "3000:3000"
    restart: always
    env_file:
      - .env
    networks:
      - ultrastream-network

networks:
  ultrastream-network:
    driver: bridge
