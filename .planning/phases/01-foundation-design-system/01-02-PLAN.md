---
phase: 01-foundation-design-system
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/styles/globals.css
  - src/styles/design-tokens.ts
  - src/components/ui/button.tsx
  - src/components/ui/card.tsx
  - src/components/ui/section.tsx
  - src/components/ui/container.tsx
  - src/components/ui/badge.tsx
  - src/components/ui/gradient-text.tsx
  - src/components/animations/custom-cursor.tsx
  - src/components/animations/hover-glow.tsx
  - src/components/animations/scroll-reveal.tsx
  - src/components/animations/micro-interactions.tsx
  - src/lib/hooks/use-media-query.ts
  - src/lib/hooks/use-mouse-position.ts
  - src/lib/gsap/gsap-provider.tsx
  - src/lib/gsap/scroll-setup.ts
  - src/app/(marketing)/page.tsx
  - src/app/(marketing)/layout.tsx
  - src/app/layout.tsx
autonomous: true

must_haves:
  truths:
    - "Custom cursor follows the mouse and changes shape on interactive elements"
    - "Hovering any button or link triggers a visible glow/scale/color-shift effect"
    - "Clicking a button produces a tactile micro-interaction (press, ripple, or flash)"
    - "Scrolling reveals content sections with animated entrances (fade, slide, scale)"
    - "Typography uses bold display font for headings and clean body font -- visually distinct hierarchy"
    - "All UI components (Button, Card, Section) use the ULTRASTREAM brand styling consistently"
    - "GSAP is initialized and ready for Phase 2 scroll-driven animations"
  artifacts:
    - path: "src/styles/design-tokens.ts"
      provides: "Typed design token system for programmatic access"
      exports: ["tokens", "gradients", "shadows"]
    - path: "src/components/ui/button.tsx"
      provides: "Brand-styled button with variants (primary, secondary, ghost, gold)"
      exports: ["Button"]
    - path: "src/components/ui/card.tsx"
      provides: "Brand-styled card with glow border and depth options"
      exports: ["Card"]
    - path: "src/components/ui/section.tsx"
      provides: "Full-width page section wrapper with spacing and optional background"
      exports: ["Section"]
    - path: "src/components/ui/container.tsx"
      provides: "Max-width centered content container"
      exports: ["Container"]
    - path: "src/components/animations/custom-cursor.tsx"
      provides: "Custom cursor overlay that tracks mouse and morphs on interactive elements"
      exports: ["CustomCursor"]
    - path: "src/components/animations/hover-glow.tsx"
      provides: "Wrapper that adds glow/scale/shift on hover"
      exports: ["HoverGlow"]
    - path: "src/components/animations/scroll-reveal.tsx"
      provides: "Wrapper that animates children into view on scroll"
      exports: ["ScrollReveal"]
    - path: "src/lib/gsap/gsap-provider.tsx"
      provides: "GSAP + ScrollTrigger + Lenis initialization"
      exports: ["GsapProvider"]
  key_links:
    - from: "src/components/ui/button.tsx"
      to: "src/styles/globals.css"
      via: "Tailwind theme tokens"
      pattern: "(neon-blue|neon-purple|gold)"
    - from: "src/components/animations/custom-cursor.tsx"
      to: "src/lib/hooks/use-mouse-position.ts"
      via: "hook import"
      pattern: "import.*useMousePosition"
    - from: "src/lib/gsap/gsap-provider.tsx"
      to: "gsap"
      via: "GSAP plugin registration"
      pattern: "gsap\\.registerPlugin"
    - from: "src/app/(marketing)/layout.tsx"
      to: "src/components/animations/custom-cursor.tsx"
      via: "component import"
      pattern: "import.*CustomCursor"
    - from: "src/app/layout.tsx"
      to: "src/lib/gsap/gsap-provider.tsx"
      via: "provider wrapping"
      pattern: "GsapProvider"
---

<objective>
Build the complete ULTRASTREAM design system: typed design tokens, brand-styled UI components (Button, Card, Section, Container), custom cursor, hover effects, micro-interactions, scroll-reveal animations, and GSAP integration -- everything needed so Phase 2 content can be dropped into a living, reactive design system.

Purpose: This transforms the dark scaffold from Plan 01 into a fully alive design system. After this plan, every interactive element responds to the user -- cursor morphs, buttons glow, sections animate in on scroll. The design system IS the brand. Phase 2+ plans consume these components without building UI primitives.

Output: Complete component library + animation system + GSAP infrastructure, demonstrated on the landing page.
</objective>

<execution_context>
@C:/Users/TDS/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/TDS/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/phases/01-foundation-design-system/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Design token system, typography, and brand UI components</name>
  <files>
    src/styles/design-tokens.ts
    src/styles/globals.css
    src/components/ui/button.tsx
    src/components/ui/card.tsx
    src/components/ui/section.tsx
    src/components/ui/container.tsx
    src/components/ui/badge.tsx
    src/components/ui/gradient-text.tsx
    src/lib/hooks/use-media-query.ts
  </files>
  <action>
    **Step 1: Create typed design tokens (`src/styles/design-tokens.ts`).**

    This file provides programmatic access to the design system for components that need JS-side token values (animations, dynamic styles, Canvas rendering). It mirrors what's in globals.css @theme but is importable in TypeScript.

    ```typescript
    export const tokens = {
      colors: {
        void: '#050505',
        abyss: '#0a0a0a',
        obsidian: '#111111',
        gunmetal: '#1a1a2e',
        slateDark: '#16213e',
        neonBlue: '#00d4ff',
        neonPurple: '#7b2ff7',
        neonRed: '#ff0040',
        neonPink: '#ff00ff',
        electricViolet: '#8b5cf6',
        gold: '#ffd700',
        goldDark: '#b8860b',
        goldLight: '#ffe55c',
        textPrimary: '#f0f0f0',
        textSecondary: '#a0a0b0',
        textMuted: '#6b7280',
      },
      spacing: {
        section: '6rem',
        sectionMobile: '3rem',
      },
      breakpoints: {
        sm: 640,
        md: 768,
        lg: 1024,
        xl: 1280,
        '2xl': 1536,
      },
      transitions: {
        easeOutExpo: 'cubic-bezier(0.16, 1, 0.3, 1)',
        easeInOutExpo: 'cubic-bezier(0.87, 0, 0.13, 1)',
        duration: {
          fast: '150ms',
          normal: '300ms',
          slow: '500ms',
          glacial: '1000ms',
        },
      },
    } as const

    export const gradients = {
      goldText: 'linear-gradient(90deg, #b8860b, #ffd700, #ffe55c)',
      neonBlue: 'linear-gradient(135deg, #00d4ff, #7b2ff7)',
      neonPurple: 'linear-gradient(135deg, #7b2ff7, #ff00ff)',
      neonRed: 'linear-gradient(135deg, #ff0040, #ff00ff)',
      darkFade: 'linear-gradient(180deg, #0a0a0a, #050505)',
      sectionDivider: 'linear-gradient(90deg, transparent, #7b2ff7, transparent)',
    } as const

    export const shadows = {
      neonBlue: '0 0 20px rgba(0, 212, 255, 0.3), 0 0 60px rgba(0, 212, 255, 0.1)',
      neonPurple: '0 0 20px rgba(123, 47, 247, 0.3), 0 0 60px rgba(123, 47, 247, 0.1)',
      gold: '0 0 20px rgba(255, 215, 0, 0.3), 0 0 60px rgba(255, 215, 0, 0.1)',
      card: '0 4px 24px rgba(0, 0, 0, 0.5)',
      cardHover: '0 8px 40px rgba(0, 0, 0, 0.7)',
    } as const
    ```

    **Step 2: Add typography utility classes to `globals.css`.**
    Append these to the existing globals.css (after the @theme block):

    ```css
    /* Typography scale */
    .text-display-hero {
      font-family: var(--font-display);
      font-weight: 700;
      font-size: clamp(3rem, 8vw, 8rem);
      line-height: 0.95;
      letter-spacing: -0.02em;
      text-transform: uppercase;
    }

    .text-display-lg {
      font-family: var(--font-display);
      font-weight: 700;
      font-size: clamp(2rem, 5vw, 4rem);
      line-height: 1.05;
      letter-spacing: -0.01em;
    }

    .text-display-md {
      font-family: var(--font-display);
      font-weight: 600;
      font-size: clamp(1.5rem, 3vw, 2.5rem);
      line-height: 1.1;
    }

    .text-display-sm {
      font-family: var(--font-display);
      font-weight: 600;
      font-size: clamp(1.125rem, 2vw, 1.5rem);
      line-height: 1.2;
    }

    .text-body-lg {
      font-family: var(--font-body);
      font-size: 1.125rem;
      line-height: 1.7;
    }

    .text-body-md {
      font-family: var(--font-body);
      font-size: 1rem;
      line-height: 1.6;
    }

    .text-body-sm {
      font-family: var(--font-body);
      font-size: 0.875rem;
      line-height: 1.5;
    }

    .text-overline {
      font-family: var(--font-body);
      font-size: 0.75rem;
      font-weight: 600;
      letter-spacing: 0.2em;
      text-transform: uppercase;
      color: var(--color-text-muted);
    }

    /* Gradient text utility */
    .text-gradient-gold {
      background: linear-gradient(90deg, var(--color-gold-dark), var(--color-gold), var(--color-gold-light));
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }

    .text-gradient-neon {
      background: linear-gradient(135deg, var(--color-neon-blue), var(--color-neon-purple));
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }

    /* Section divider */
    .section-divider {
      height: 1px;
      background: linear-gradient(90deg, transparent, var(--color-neon-purple), transparent);
      opacity: 0.3;
    }
    ```

    **Step 3: Build `src/components/ui/button.tsx`.**
    Client component ('use client') for click interactions.

    Variants:
    - `primary`: Neon blue/purple gradient background, white text. Hover: glow intensifies, slight scale up.
    - `secondary`: Transparent with neon border. Hover: border glows, background fills slightly.
    - `ghost`: No border, subtle text. Hover: text brightens, underline slides in.
    - `gold`: Gold gradient background, dark text. Hover: gold glow, slight scale.
    - `danger`: Neon red background. Hover: red glow.

    Sizes: `sm`, `md`, `lg`, `xl`

    All variants include:
    - Transition on hover (300ms ease-out-expo)
    - Active state: scale(0.97) for tactile press feel
    - Focus-visible ring with neon color
    - Disabled state: opacity 50%, no interactions
    - `group` class for child animations

    ```tsx
    'use client'

    import { forwardRef, type ButtonHTMLAttributes } from 'react'
    import { cn } from '@/lib/utils/cn'

    interface ButtonProps extends ButtonHTMLAttributes<HTMLButtonElement> {
      variant?: 'primary' | 'secondary' | 'ghost' | 'gold' | 'danger'
      size?: 'sm' | 'md' | 'lg' | 'xl'
    }

    const variants = {
      primary: [
        'bg-gradient-to-r from-neon-blue to-neon-purple text-white',
        'hover:shadow-[var(--shadow-neon-blue)] hover:scale-[1.02]',
        'active:scale-[0.97]',
        'focus-visible:ring-2 focus-visible:ring-neon-blue focus-visible:ring-offset-2 focus-visible:ring-offset-abyss',
      ].join(' '),
      secondary: [
        'bg-transparent border border-neon-purple/50 text-neon-purple',
        'hover:border-neon-purple hover:bg-neon-purple/10 hover:shadow-[var(--shadow-neon-purple)]',
        'active:scale-[0.97]',
        'focus-visible:ring-2 focus-visible:ring-neon-purple focus-visible:ring-offset-2 focus-visible:ring-offset-abyss',
      ].join(' '),
      ghost: [
        'bg-transparent text-text-secondary',
        'hover:text-text-primary hover:bg-white/5',
        'active:scale-[0.97]',
        'focus-visible:ring-2 focus-visible:ring-white/20 focus-visible:ring-offset-2 focus-visible:ring-offset-abyss',
      ].join(' '),
      gold: [
        'bg-gradient-to-r from-gold-dark via-gold to-gold-light text-void font-semibold',
        'hover:shadow-[var(--shadow-gold)] hover:scale-[1.02]',
        'active:scale-[0.97]',
        'focus-visible:ring-2 focus-visible:ring-gold focus-visible:ring-offset-2 focus-visible:ring-offset-abyss',
      ].join(' '),
      danger: [
        'bg-neon-red text-white',
        'hover:shadow-[0_0_20px_rgba(255,0,64,0.4)] hover:scale-[1.02]',
        'active:scale-[0.97]',
        'focus-visible:ring-2 focus-visible:ring-neon-red focus-visible:ring-offset-2 focus-visible:ring-offset-abyss',
      ].join(' '),
    }

    const sizes = {
      sm: 'px-3 py-1.5 text-sm rounded-[var(--radius-sm)]',
      md: 'px-5 py-2.5 text-sm rounded-[var(--radius-md)]',
      lg: 'px-7 py-3 text-base rounded-[var(--radius-lg)]',
      xl: 'px-10 py-4 text-lg rounded-[var(--radius-xl)] font-display tracking-wide uppercase',
    }

    export const Button = forwardRef<HTMLButtonElement, ButtonProps>(
      ({ variant = 'primary', size = 'md', className, disabled, children, ...props }, ref) => {
        return (
          <button
            ref={ref}
            disabled={disabled}
            className={cn(
              'inline-flex items-center justify-center gap-2 font-medium',
              'transition-all duration-300',
              '[transition-timing-function:var(--ease-out-expo)]',
              'cursor-pointer select-none',
              'disabled:opacity-50 disabled:pointer-events-none',
              variants[variant],
              sizes[size],
              className
            )}
            {...props}
          >
            {children}
          </button>
        )
      }
    )
    Button.displayName = 'Button'
    ```

    **Step 4: Build `src/components/ui/card.tsx`.**
    Styled card component matching the brand.

    Props:
    - `variant?: 'default' | 'elevated' | 'bordered'`
    - `glow?: 'blue' | 'purple' | 'gold' | 'none'`
    - `hover?: boolean` (enable hover lift effect)
    - Standard div props

    Implementation:
    - `default`: bg-obsidian/60 with subtle border border-white/5, backdrop-blur
    - `elevated`: bg-obsidian/80, stronger shadow, more prominent border
    - `bordered`: transparent bg, prominent neon border based on glow color
    - Hover (when enabled): translateY(-2px), shadow intensifies, border brightens
    - All cards have rounded-lg, p-6, transition-all duration-300

    **Step 5: Build `src/components/ui/section.tsx`.**
    Full-width page section wrapper.

    Props:
    - `id?: string` (for anchor links)
    - `background?: 'default' | 'gradient' | 'darker' | 'accent'`
    - `spacing?: 'sm' | 'md' | 'lg'`
    - `divider?: boolean` (show gradient divider line at bottom)
    - Standard div props

    Implementation:
    - Handles vertical spacing: sm = py-12, md = py-20, lg = py-28 (responsive)
    - `gradient` background: subtle radial gradient from center (purple tint)
    - `darker` background: bg-void instead of bg-abyss
    - `accent` background: very subtle neon-purple wash
    - When `divider` is true, renders the section-divider line at the bottom

    **Step 6: Build `src/components/ui/container.tsx`.**
    Max-width centered content container.

    Props:
    - `size?: 'sm' | 'md' | 'lg' | 'xl' | 'full'`
    - Standard div props

    Sizes: sm = max-w-2xl, md = max-w-4xl, lg = max-w-6xl, xl = max-w-7xl, full = max-w-[1440px]
    All centered with mx-auto, px-4 md:px-8

    **Step 7: Build `src/components/ui/badge.tsx`.**
    Small label/tag component.

    Variants:
    - `default`: bg-white/5, text-text-secondary, border border-white/10
    - `neon`: bg-neon-purple/10, text-neon-purple, border border-neon-purple/30
    - `gold`: bg-gold/10, text-gold, border border-gold/30
    - `live`: bg-neon-red, text-white, with a pulsing dot animation

    All are small (text-xs, px-2.5 py-0.5, rounded-full) with font-medium.

    **Step 8: Build `src/components/ui/gradient-text.tsx`.**
    Reusable gradient text component.

    Props:
    - `variant?: 'gold' | 'neon' | 'fire' | 'custom'`
    - `gradient?: string` (custom gradient for 'custom' variant)
    - `as?: 'span' | 'h1' | 'h2' | 'h3' | 'p'` (polymorphic element)

    Each variant maps to a background gradient applied with bg-clip-text and text-transparent.

    **Step 9: Build `src/lib/hooks/use-media-query.ts`.**
    Custom hook for responsive behavior in client components.

    ```typescript
    'use client'
    import { useState, useEffect } from 'react'

    export function useMediaQuery(query: string): boolean {
      const [matches, setMatches] = useState(false)

      useEffect(() => {
        const media = window.matchMedia(query)
        setMatches(media.matches)
        const listener = (e: MediaQueryListEvent) => setMatches(e.matches)
        media.addEventListener('change', listener)
        return () => media.removeEventListener('change', listener)
      }, [query])

      return matches
    }

    export const useIsMobile = () => useMediaQuery('(max-width: 767px)')
    export const useIsTablet = () => useMediaQuery('(min-width: 768px) and (max-width: 1023px)')
    export const useIsDesktop = () => useMediaQuery('(min-width: 1024px)')
    ```
  </action>
  <verify>
    1. `pnpm build` succeeds with no TypeScript errors
    2. Import `{ Button }` from `@/components/ui/button` works in a test page
    3. All button variants render with correct styling (primary has gradient, gold has gold bg, etc.)
    4. Button press produces visible scale-down (active:scale-[0.97])
    5. Card component renders with dark background and subtle border
    6. Section component applies correct spacing and optional gradient background
    7. Badge "live" variant shows pulsing red dot
    8. design-tokens.ts exports typed token objects without errors
  </verify>
  <done>
    Complete design token system (CSS + TypeScript), typography scale, and 6 brand-styled UI components (Button, Card, Section, Container, Badge, GradientText) are built and consistent with ULTRASTREAM's dark, heavy-metal visual identity. All components use theme tokens. Typography hierarchy is clear (display hero through body-sm). Components are ready for Phase 2+ content.
  </done>
</task>

<task type="auto">
  <name>Task 2: Custom cursor, hover effects, and micro-interaction animations</name>
  <files>
    src/components/animations/custom-cursor.tsx
    src/components/animations/hover-glow.tsx
    src/components/animations/micro-interactions.tsx
    src/lib/hooks/use-mouse-position.ts
    src/app/(marketing)/layout.tsx
  </files>
  <action>
    **Step 1: Build `src/lib/hooks/use-mouse-position.ts`.**
    Custom hook to track mouse position with requestAnimationFrame smoothing.

    ```typescript
    'use client'
    import { useState, useEffect, useRef } from 'react'

    interface MousePosition {
      x: number
      y: number
      isMoving: boolean
    }

    export function useMousePosition(): MousePosition {
      const [position, setPosition] = useState<MousePosition>({ x: 0, y: 0, isMoving: false })
      const timeoutRef = useRef<ReturnType<typeof setTimeout>>()

      useEffect(() => {
        const handler = (e: MouseEvent) => {
          setPosition({ x: e.clientX, y: e.clientY, isMoving: true })

          if (timeoutRef.current) clearTimeout(timeoutRef.current)
          timeoutRef.current = setTimeout(() => {
            setPosition(prev => ({ ...prev, isMoving: false }))
          }, 150)
        }

        window.addEventListener('mousemove', handler)
        return () => {
          window.removeEventListener('mousemove', handler)
          if (timeoutRef.current) clearTimeout(timeoutRef.current)
        }
      }, [])

      return position
    }
    ```

    **Step 2: Build `src/components/animations/custom-cursor.tsx`.**
    Client component that replaces the default cursor on desktop with a custom ULTRASTREAM cursor.

    Implementation:
    - ONLY renders on desktop (use `useMediaQuery` to detect -- no custom cursor on touch devices)
    - Two layers:
      1. **Dot**: Small (8px) circle that follows mouse instantly. Color: neon-blue.
      2. **Ring**: Larger (40px) circle that follows with a slight delay (lerp/spring). Color: neon-purple with low opacity.
    - The ring EXPANDS when hovering over interactive elements (buttons, links, cards with hover):
      - Detect by adding a global event listener for `mouseover` on elements with `data-cursor="pointer"` attribute, or elements matching `a, button, [role="button"], input, textarea, select, [data-interactive]`
      - When over interactive: ring grows to 60px, changes color to gold, opacity increases
      - When over text: ring shrinks slightly, shows a text-select indicator
    - Use CSS `pointer-events: none` on the cursor so it doesn't block clicks
    - Use CSS `mix-blend-mode: difference` for the ring to create a visual effect against content
    - Position with `transform: translate()` for GPU-accelerated movement
    - Hide the default cursor with `cursor: none` applied via globals.css on desktop only:
      ```css
      @media (pointer: fine) {
        * { cursor: none !important; }
      }
      ```

    Smooth following using requestAnimationFrame:
    ```tsx
    'use client'

    import { useEffect, useRef, useState } from 'react'
    import { cn } from '@/lib/utils/cn'
    import { useMediaQuery } from '@/lib/hooks/use-media-query'

    export function CustomCursor() {
      const isDesktop = useMediaQuery('(pointer: fine)')
      const dotRef = useRef<HTMLDivElement>(null)
      const ringRef = useRef<HTMLDivElement>(null)
      const [isHovering, setIsHovering] = useState(false)
      const mouse = useRef({ x: 0, y: 0 })
      const ringPos = useRef({ x: 0, y: 0 })

      useEffect(() => {
        if (!isDesktop) return

        const onMouseMove = (e: MouseEvent) => {
          mouse.current = { x: e.clientX, y: e.clientY }
          if (dotRef.current) {
            dotRef.current.style.transform = `translate(${e.clientX - 4}px, ${e.clientY - 4}px)`
          }
        }

        const onMouseOver = (e: MouseEvent) => {
          const target = e.target as HTMLElement
          const isInteractive = target.closest('a, button, [role="button"], input, textarea, select, [data-interactive]')
          setIsHovering(!!isInteractive)
        }

        // Smooth ring follow with lerp
        let animFrame: number
        const animate = () => {
          ringPos.current.x += (mouse.current.x - ringPos.current.x) * 0.15
          ringPos.current.y += (mouse.current.y - ringPos.current.y) * 0.15
          if (ringRef.current) {
            const size = isHovering ? 60 : 40
            ringRef.current.style.transform = `translate(${ringPos.current.x - size / 2}px, ${ringPos.current.y - size / 2}px)`
          }
          animFrame = requestAnimationFrame(animate)
        }

        window.addEventListener('mousemove', onMouseMove)
        document.addEventListener('mouseover', onMouseOver)
        animFrame = requestAnimationFrame(animate)

        return () => {
          window.removeEventListener('mousemove', onMouseMove)
          document.removeEventListener('mouseover', onMouseOver)
          cancelAnimationFrame(animFrame)
        }
      }, [isDesktop, isHovering])

      if (!isDesktop) return null

      return (
        <>
          {/* Dot - instant follow */}
          <div
            ref={dotRef}
            className="fixed top-0 left-0 z-[var(--z-cursor)] pointer-events-none w-2 h-2 rounded-full bg-neon-blue"
            style={{ willChange: 'transform' }}
          />
          {/* Ring - delayed follow */}
          <div
            ref={ringRef}
            className={cn(
              'fixed top-0 left-0 z-[var(--z-cursor)] pointer-events-none rounded-full',
              'border transition-[width,height,border-color,opacity] duration-300',
              isHovering
                ? 'w-[60px] h-[60px] border-gold/60 opacity-80'
                : 'w-[40px] h-[40px] border-neon-purple/30 opacity-50'
            )}
            style={{
              willChange: 'transform',
              mixBlendMode: 'difference',
            }}
          />
        </>
      )
    }
    ```

    **Step 3: Build `src/components/animations/hover-glow.tsx`.**
    A wrapper component that adds interactive hover effects to any child.

    Props:
    - `color?: 'blue' | 'purple' | 'gold' | 'red'` (glow color)
    - `intensity?: 'subtle' | 'medium' | 'strong'`
    - `scale?: boolean` (enable scale on hover, default true)

    Implementation using Motion (Framer Motion):
    ```tsx
    'use client'

    import { motion } from 'motion/react'
    import { cn } from '@/lib/utils/cn'

    interface HoverGlowProps {
      children: React.ReactNode
      color?: 'blue' | 'purple' | 'gold' | 'red'
      intensity?: 'subtle' | 'medium' | 'strong'
      scale?: boolean
      className?: string
    }

    const glowMap = {
      blue: { base: 'rgba(0, 212, 255, 0)', hover: 'rgba(0, 212, 255, 0.15)' },
      purple: { base: 'rgba(123, 47, 247, 0)', hover: 'rgba(123, 47, 247, 0.15)' },
      gold: { base: 'rgba(255, 215, 0, 0)', hover: 'rgba(255, 215, 0, 0.12)' },
      red: { base: 'rgba(255, 0, 64, 0)', hover: 'rgba(255, 0, 64, 0.15)' },
    }

    const intensityScale = {
      subtle: 1,
      medium: 1.02,
      strong: 1.05,
    }

    export function HoverGlow({
      children,
      color = 'purple',
      intensity = 'medium',
      scale = true,
      className,
    }: HoverGlowProps) {
      return (
        <motion.div
          className={cn('relative', className)}
          data-interactive
          whileHover={{
            scale: scale ? intensityScale[intensity] : 1,
            backgroundColor: glowMap[color].hover,
          }}
          transition={{
            type: 'spring',
            stiffness: 400,
            damping: 25,
          }}
        >
          {children}
        </motion.div>
      )
    }
    ```

    **Step 4: Build `src/components/animations/micro-interactions.tsx`.**
    A collection of small interaction components:

    **`PressEffect`**: Wraps a button/element to add press animation.
    ```tsx
    export function PressEffect({ children, className }: { children: React.ReactNode; className?: string }) {
      return (
        <motion.div
          className={className}
          whileTap={{ scale: 0.95 }}
          transition={{ type: 'spring', stiffness: 500, damping: 30 }}
        >
          {children}
        </motion.div>
      )
    }
    ```

    **`RippleButton`**: Button with a radial ripple effect on click (like Material Design but neon-colored).
    - On click, create a radial expanding circle from the click position
    - Circle starts small, expands to fill the button, then fades out
    - Color matches the button's glow color (neon-blue by default)
    - Use CSS animation for the ripple (performant, GPU-accelerated)

    **`FloatEffect`**: Wraps an element with a subtle floating animation (up/down bob).
    ```tsx
    export function FloatEffect({ children, delay = 0, className }: { children: React.ReactNode; delay?: number; className?: string }) {
      return (
        <motion.div
          className={className}
          animate={{ y: [0, -8, 0] }}
          transition={{
            duration: 3,
            repeat: Infinity,
            repeatType: 'loop',
            ease: 'easeInOut',
            delay,
          }}
        >
          {children}
        </motion.div>
      )
    }
    ```

    **`PulseGlow`**: Wraps an element with a pulsing glow outline effect.
    ```tsx
    export function PulseGlow({ children, color = 'purple', className }: { children: React.ReactNode; color?: string; className?: string }) {
      return (
        <motion.div
          className={cn('relative', className)}
          animate={{
            boxShadow: [
              `0 0 0px rgba(123, 47, 247, 0)`,
              `0 0 20px rgba(123, 47, 247, 0.3)`,
              `0 0 0px rgba(123, 47, 247, 0)`,
            ],
          }}
          transition={{ duration: 2, repeat: Infinity }}
        >
          {children}
        </motion.div>
      )
    }
    ```

    **Step 5: Update `src/app/(marketing)/layout.tsx` to include the CustomCursor.**
    Import and render `<CustomCursor />` in the marketing layout. It renders as a fixed overlay so it appears across all marketing pages.

    Also add the cursor-hiding CSS to globals.css:
    ```css
    @media (pointer: fine) {
      .cursor-custom * {
        cursor: none !important;
      }
    }
    ```
    Add `cursor-custom` class to the marketing layout wrapper.
  </action>
  <verify>
    1. Moving the mouse on desktop shows a small neon-blue dot following instantly and a larger purple ring following with delay
    2. Hovering over a button/link causes the cursor ring to expand and turn gold
    3. On mobile viewport (or touch devices), no custom cursor renders
    4. Wrapping a div in `<HoverGlow>` produces a visible purple glow + scale on hover
    5. `<PressEffect>` produces visible scale-down on click/tap
    6. `<FloatEffect>` creates smooth bobbing animation
    7. `<PulseGlow>` creates a repeating glow pulse
    8. No performance issues -- check for layout thrashing in Performance tab
  </verify>
  <done>
    Custom cursor tracks the mouse with a neon-blue dot and purple ring, morphing on interactive elements. HoverGlow, PressEffect, FloatEffect, and PulseGlow micro-interaction components are built and working. Every interactive element on the site now responds visually to user input -- the site feels alive and reactive, matching BRAND-06 requirements.
  </done>
</task>

<task type="auto">
  <name>Task 3: GSAP + Lenis integration, scroll-reveal animations, and showcase page</name>
  <files>
    src/lib/gsap/gsap-provider.tsx
    src/lib/gsap/scroll-setup.ts
    src/components/animations/scroll-reveal.tsx
    src/app/(marketing)/page.tsx
    src/app/(marketing)/layout.tsx
    src/app/layout.tsx
  </files>
  <action>
    **Step 1: Build GSAP + Lenis provider (`src/lib/gsap/gsap-provider.tsx`).**

    This is the animation infrastructure for the entire site. It initializes GSAP plugins, sets up Lenis smooth scroll, and connects them together.

    ```tsx
    'use client'

    import { useEffect, useRef } from 'react'
    import gsap from 'gsap'
    import { ScrollTrigger } from 'gsap/ScrollTrigger'
    import { useGSAP } from '@gsap/react'
    import Lenis from 'lenis'

    // Register GSAP plugins
    if (typeof window !== 'undefined') {
      gsap.registerPlugin(ScrollTrigger)
    }

    interface GsapProviderProps {
      children: React.ReactNode
    }

    export function GsapProvider({ children }: GsapProviderProps) {
      const lenisRef = useRef<Lenis | null>(null)

      useEffect(() => {
        // Initialize Lenis smooth scroll
        const lenis = new Lenis({
          duration: 1.2,
          easing: (t) => Math.min(1, 1.001 - Math.pow(2, -10 * t)), // easeOutExpo
          orientation: 'vertical',
          gestureOrientation: 'vertical',
          smoothWheel: true,
          touchMultiplier: 2,
        })

        lenisRef.current = lenis

        // Connect Lenis to GSAP ScrollTrigger
        lenis.on('scroll', ScrollTrigger.update)

        // Use GSAP ticker for Lenis animation loop
        gsap.ticker.add((time) => {
          lenis.raf(time * 1000)
        })

        // Disable GSAP's built-in lag smoothing to sync with Lenis
        gsap.ticker.lagSmoothing(0)

        return () => {
          lenis.destroy()
          lenisRef.current = null
        }
      }, [])

      return <>{children}</>
    }
    ```

    **Step 2: Create GSAP scroll utilities (`src/lib/gsap/scroll-setup.ts`).**

    Export reusable GSAP animation presets that Phase 2+ can use:

    ```typescript
    import gsap from 'gsap'
    import { ScrollTrigger } from 'gsap/ScrollTrigger'

    /**
     * Default ScrollTrigger settings for section reveals
     */
    export const defaultScrollTrigger = (trigger: string | Element) => ({
      trigger,
      start: 'top 80%',
      end: 'top 20%',
      toggleActions: 'play none none reverse',
    })

    /**
     * Preset: Fade up and in
     */
    export const fadeUpPreset = {
      from: { opacity: 0, y: 60 },
      to: { opacity: 1, y: 0, duration: 0.8, ease: 'power3.out' },
    }

    /**
     * Preset: Fade in from left
     */
    export const fadeLeftPreset = {
      from: { opacity: 0, x: -60 },
      to: { opacity: 1, x: 0, duration: 0.8, ease: 'power3.out' },
    }

    /**
     * Preset: Fade in from right
     */
    export const fadeRightPreset = {
      from: { opacity: 0, x: 60 },
      to: { opacity: 1, x: 0, duration: 0.8, ease: 'power3.out' },
    }

    /**
     * Preset: Scale up and in
     */
    export const scaleUpPreset = {
      from: { opacity: 0, scale: 0.9 },
      to: { opacity: 1, scale: 1, duration: 0.6, ease: 'back.out(1.5)' },
    }

    /**
     * Preset: Staggered children reveal
     */
    export const staggerPreset = {
      from: { opacity: 0, y: 40 },
      to: { opacity: 1, y: 0, duration: 0.5, ease: 'power2.out', stagger: 0.1 },
    }
    ```

    **Step 3: Build `src/components/animations/scroll-reveal.tsx`.**

    A reusable component that animates children into view when they scroll into the viewport.

    Props:
    - `animation?: 'fadeUp' | 'fadeLeft' | 'fadeRight' | 'scaleUp' | 'custom'`
    - `delay?: number`
    - `duration?: number`
    - `stagger?: number` (if children should stagger)
    - `threshold?: number` (how much of element must be visible, 0-1)
    - `once?: boolean` (only animate once, default true)

    Implementation using `useGSAP` hook from `@gsap/react`:

    ```tsx
    'use client'

    import { useRef } from 'react'
    import gsap from 'gsap'
    import { ScrollTrigger } from 'gsap/ScrollTrigger'
    import { useGSAP } from '@gsap/react'
    import { cn } from '@/lib/utils/cn'

    interface ScrollRevealProps {
      children: React.ReactNode
      animation?: 'fadeUp' | 'fadeLeft' | 'fadeRight' | 'scaleUp'
      delay?: number
      duration?: number
      stagger?: number
      once?: boolean
      className?: string
    }

    const animationConfigs = {
      fadeUp: { y: 60, opacity: 0 },
      fadeLeft: { x: -60, opacity: 0 },
      fadeRight: { x: 60, opacity: 0 },
      scaleUp: { scale: 0.9, opacity: 0 },
    }

    export function ScrollReveal({
      children,
      animation = 'fadeUp',
      delay = 0,
      duration = 0.8,
      stagger,
      once = true,
      className,
    }: ScrollRevealProps) {
      const containerRef = useRef<HTMLDivElement>(null)

      useGSAP(() => {
        if (!containerRef.current) return

        const targets = stagger
          ? containerRef.current.children
          : containerRef.current

        gsap.from(targets, {
          ...animationConfigs[animation],
          duration,
          delay,
          stagger: stagger || 0,
          ease: 'power3.out',
          scrollTrigger: {
            trigger: containerRef.current,
            start: 'top 85%',
            toggleActions: once
              ? 'play none none none'
              : 'play none none reverse',
          },
        })
      }, { scope: containerRef })

      return (
        <div ref={containerRef} className={cn(className)}>
          {children}
        </div>
      )
    }
    ```

    **Step 4: Add GsapProvider to root layout (`src/app/layout.tsx`).**
    Wrap the page content in `<GsapProvider>` after the ThemeProvider. This initializes Lenis smooth scroll and GSAP on all pages.

    **Step 5: Update the marketing layout (`src/app/(marketing)/layout.tsx`).**
    Ensure CustomCursor is imported and rendered.

    **Step 6: Rebuild the showcase landing page (`src/app/(marketing)/page.tsx`).**
    Replace the placeholder content with a comprehensive design system showcase that demonstrates ALL components and animations built in this plan. This page serves as both the landing page placeholder AND a visual test of the design system.

    Structure:
    ```
    1. HERO SECTION (full viewport height)
       - ULTRASTREAM logo in text-display-hero with gradient-gold
       - "THE STREAMING REVOLUTION" in text-display-md with neon text
       - Tagline: "For the people. By the people. Against the machine."
       - Two Buttons: "Join the Revolution" (gold, xl) and "Learn More" (secondary, lg)
       - Wrapped in ScrollReveal fadeUp
       - Badge: "COMING SOON" with neon variant

    2. FEATURES GRID (GridSystem with GridCells)
       - 4 feature cards using Card component with different glow colors
       - Each wrapped in ScrollReveal with stagger
       - Each wrapped in HoverGlow
       - Content: Creator Monetization, Viewer Rewards, Discovery Engine, Censorship Resistance
       - Section component with divider

    3. REBELLION SECTION (full-width statement)
       - Large GradientText: "WE'RE TAKING STREAMING BACK"
       - Paragraph about the mission in text-body-lg
       - ScrollReveal scaleUp animation
       - PulseGlow on the heading

    4. STATS ROW
       - Three stat cards in a row: "0-5%" (fee), "100%" (creator owned), "0" (censorship)
       - FloatEffect on each with staggered delays
       - ScrollReveal fadeUp with stagger

    5. CTA SECTION
       - "Join the Revolution" heading
       - Placeholder for waitlist form
       - Gold button with PressEffect wrapper
       - Section with accent background
    ```

    This page demonstrates to the user (and to Phase 2) that every component works: buttons, cards, sections, grid, scroll reveals, hover effects, micro-interactions, cursor, smooth scrolling. It's a living style guide AND a landing page skeleton.
  </action>
  <verify>
    1. Page loads with Lenis smooth scroll active (scroll feels buttery smooth, not native jerky)
    2. Scrolling down reveals sections with fadeUp/scaleUp animations firing at the right scroll positions
    3. Feature cards stagger in one after another when their section enters the viewport
    4. HoverGlow effect works on feature cards (visible glow + scale on hover)
    5. Buttons show press effect (scale down) on click
    6. FloatEffect stat cards bob gently up and down
    7. PulseGlow creates visible repeating glow on the rebellion heading
    8. Custom cursor works across the entire page (dot + ring)
    9. Cursor ring expands on buttons and links
    10. `pnpm build` succeeds -- no TypeScript errors, no build warnings about unused imports
    11. Mobile: scroll reveals still fire, no custom cursor, single-column layout, smooth scroll works
    12. Performance: scroll at 60fps on desktop (check Performance tab -- no long frames)
  </verify>
  <done>
    GSAP + Lenis smooth scroll infrastructure is initialized and working. ScrollReveal component animates content on scroll with multiple animation presets. The landing page showcases every design system component: Buttons (5 variants), Cards (with glow), Sections (with dividers), Grid layout, ScrollReveal animations, HoverGlow effects, micro-interactions (PressEffect, FloatEffect, PulseGlow), and custom cursor. The site is alive with interactions -- BRAND-04 (animations) and BRAND-06 (cursor + hover + micro-interactions) requirements are met. GSAP presets are exported for Phase 2 scroll-driven animations.
  </done>
</task>

</tasks>

<verification>
Phase 1, Plan 02 verification checklist:

**BRAND-01 (Dark premium color palette):**
- [ ] All UI components use the ULTRASTREAM color tokens (abyss, obsidian, neon accents, gold)
- [ ] No default Tailwind gray/blue colors leaking through
- [ ] Neon glows visible on cards and buttons

**BRAND-02 (Typography system):**
- [ ] Display font (Oswald) used for all headings -- visually distinct from body
- [ ] Body font (Inter) used for paragraph text -- clean and readable
- [ ] Typography scale classes work (text-display-hero through text-body-sm)
- [ ] Gradient text (gold, neon) renders correctly

**BRAND-04 (Section transition animations):**
- [ ] Scrolling triggers smooth section reveals (not instant pop-in)
- [ ] Multiple animation types work (fadeUp, fadeLeft, scaleUp)
- [ ] Animations feel premium -- smooth easing, appropriate duration
- [ ] Lenis smooth scroll is active (butter-smooth vs native)

**BRAND-05 (Responsive design):**
- [ ] All components render correctly on mobile, tablet, desktop
- [ ] Custom cursor only appears on desktop (pointer: fine)
- [ ] Typography scales responsively (clamp-based)

**BRAND-06 (Custom cursor, hover effects, micro-interactions):**
- [ ] Custom cursor follows mouse with neon dot + ring
- [ ] Cursor ring morphs on interactive elements (grows, changes color)
- [ ] Hover effects visible on all interactive elements (buttons, cards, links)
- [ ] Click/press produces tactile micro-interaction (scale down)
- [ ] Float and pulse effects work for ambient motion

**ANIM-04 (continued from Plan 01):**
- [ ] Preloader still works correctly with the new animation system
- [ ] No conflict between CSS preloader animations and GSAP/Motion animations
</verification>

<success_criteria>
1. Design token system provides both CSS variables (globals.css @theme) and TypeScript exports (design-tokens.ts)
2. All 6 UI components (Button, Card, Section, Container, Badge, GradientText) render with ULTRASTREAM brand styling
3. Button has 5 working variants (primary, secondary, ghost, gold, danger) with hover/active/focus states
4. Custom cursor tracks mouse on desktop, morphs on interactive elements, hidden on mobile
5. HoverGlow, PressEffect, FloatEffect, PulseGlow micro-interactions all work
6. ScrollReveal animates content on scroll using GSAP ScrollTrigger
7. Lenis smooth scroll is active and feels buttery
8. GSAP presets are exported for Phase 2 consumption
9. Landing page showcases all components in a cohesive design-system demo
10. `pnpm build` succeeds with zero errors
11. Site deploys to Vercel (auto-deploy from git push) with all features working
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-design-system/01-02-SUMMARY.md`
</output>
