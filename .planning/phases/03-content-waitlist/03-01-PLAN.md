---
phase: 03-content-waitlist
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/VillainsSection.tsx
  - src/components/sections/RevenueComparison.tsx
  - src/components/sections/FeaturesShowcase.tsx
  - src/components/sections/UltraverseSection.tsx
  - src/components/ui/VillainCard.tsx
  - src/components/ui/FeatureCard.tsx
  - src/components/ui/RevenueBar.tsx
  - src/lib/content/villains.ts
  - src/lib/content/features.ts
  - src/app/page.tsx
autonomous: true

must_haves:
  truths:
    - "Visitor sees 4 competitor pain points (Twitch, YouTube, TikTok, Discord) each with a visceral problem statement and ULTRASTREAM solution"
    - "Revenue comparison visual shows ULTRASTREAM 0-5% vs Twitch 50% vs YouTube 30% in an immediately understandable animated chart"
    - "All 6 feature sections render with human-language descriptions, icons, and scroll-triggered GSAP animations"
    - "ULTRAVERSE.games ecosystem connection section exists and positions ULTRASTREAM within the parent platform"
    - "Anti-corporate rally tone is consistent throughout -- built for the people, not a crypto template"
  artifacts:
    - path: "src/components/sections/VillainsSection.tsx"
      provides: "Pain point storytelling section with 4 villain cards"
      min_lines: 80
    - path: "src/components/sections/RevenueComparison.tsx"
      provides: "Animated revenue comparison visualization"
      min_lines: 60
    - path: "src/components/sections/FeaturesShowcase.tsx"
      provides: "6 feature showcase sections with scroll animations"
      min_lines: 120
    - path: "src/components/sections/UltraverseSection.tsx"
      provides: "ULTRAVERSE.games ecosystem connection"
      min_lines: 40
    - path: "src/lib/content/villains.ts"
      provides: "Villain data with pain points and solutions"
      exports: ["villains"]
    - path: "src/lib/content/features.ts"
      provides: "Feature data for all 6 platform features"
      exports: ["features"]
  key_links:
    - from: "src/components/sections/VillainsSection.tsx"
      to: "src/lib/content/villains.ts"
      via: "import villains data array"
      pattern: "import.*villains"
    - from: "src/components/sections/FeaturesShowcase.tsx"
      to: "src/lib/content/features.ts"
      via: "import features data array"
      pattern: "import.*features"
    - from: "src/components/sections/VillainsSection.tsx"
      to: "gsap"
      via: "GSAP ScrollTrigger for card reveal animations"
      pattern: "ScrollTrigger|gsap\\.to|gsap\\.from"
    - from: "src/components/sections/RevenueComparison.tsx"
      to: "gsap"
      via: "GSAP animation for revenue bar growth"
      pattern: "ScrollTrigger|gsap\\.to"
    - from: "src/app/page.tsx"
      to: "src/components/sections/VillainsSection.tsx"
      via: "section import and render in page layout"
      pattern: "import.*VillainsSection"
---

<objective>
Build the pain point storytelling ("The Villains") and platform feature showcase sections that form the marketing content backbone of the ULTRASTREAM website.

Purpose: This is the persuasion engine. Visitors must feel the pain of current platforms viscerally, see ULTRASTREAM as the weapon that kills each villain, and understand all 6 platform features in human language -- no crypto jargon. The tone is rally-the-troops, Conan killing the bad guys, "WE are doing something together."

Output: 4 content sections (Villains, Revenue Comparison, Features Showcase, ULTRAVERSE Ecosystem) integrated into the main page with GSAP scroll animations.
</objective>

<execution_context>
@C:/Users/TDS/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/TDS/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/FEATURES.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Villain pain point cards and revenue comparison</name>
  <files>
    src/lib/content/villains.ts
    src/components/ui/VillainCard.tsx
    src/components/ui/RevenueBar.tsx
    src/components/sections/VillainsSection.tsx
    src/components/sections/RevenueComparison.tsx
  </files>
  <action>
Create the content data and UI components for "The Problem" section.

**Content data file `src/lib/content/villains.ts`:**
Export a `villains` array of 4 objects, each with:
- `id`: "twitch" | "youtube" | "tiktok" | "discord"
- `name`: Platform name
- `icon`: Path or component reference for competitor logo/icon (use simple SVG icons or emoji placeholders -- do NOT import external logo assets that could have trademark issues; use stylized text or abstract icons representing each platform's pain)
- `color`: Brand-associated accent color (purple for Twitch, red for YouTube, cyan for TikTok, indigo for Discord)
- `painPoint`: The villain headline -- visceral, angry, specific. Examples:
  - Twitch: "TAKES 50% OF EVERYTHING YOU EARN"
  - YouTube: "AN ALGORITHM THAT DECIDES IF YOU EXIST"
  - TikTok: "CENSORS YOUR VOICE, SELLS YOUR DATA"
  - Discord: "ZERO DISCOVERY. YOUR COMMUNITY IS A WALLED GRAVE."
- `details`: 2-3 sentence expansion using real data from FEATURES.md research (e.g., "Standard affiliates lose 50% of every $4.99 sub. Even Partner Plus 70/30 is temporary and requires sustained sub counts.")
- `solution`: ULTRASTREAM's weapon that kills this villain. Use human language:
  - vs Twitch: "Keep 95-100% of your earnings. Automatic payments via smart contracts -- no middleman taking half."
  - vs YouTube: "Our discovery engine boosts new creators. Your content gets seen based on community love, not corporate algorithms."
  - vs TikTok: "Decentralized delivery means no single authority can silence you. Community-run rules, not corporate censorship."
  - vs Discord: "Built-in discovery from day one. Your community isn't hidden behind invite links -- it's part of a living ecosystem."

**VillainCard component `src/components/ui/VillainCard.tsx`:**
A card component that shows the villain side (pain point) and flips/transitions to reveal the ULTRASTREAM solution side. Props: villain data object. Use GSAP for the flip animation triggered by ScrollTrigger (card enters viewport -> villain side visible, scrolls further -> flips to solution). The villain side should feel oppressive (darker, red/warning tones), the solution side should feel liberating (brand accent colors, brighter). Include the competitor's stylized icon and ULTRASTREAM's response icon.

**RevenueBar component `src/components/ui/RevenueBar.tsx`:**
A single animated bar component. Props: `platform` (string), `percentage` (number for creator's keep), `color` (string), `label` (string). The bar animates from 0 to its target width via GSAP ScrollTrigger when it enters the viewport. Show the percentage prominently. Use the design system's typography and colors.

**VillainsSection `src/components/sections/VillainsSection.tsx`:**
The full "THE PROBLEM" section layout. Section heading: something like "THE VILLAINS" or "THEY'VE BEEN SCREWING YOU" -- rally-the-troops tone, not corporate. Subheading: "Every major platform takes your money, hides your content, or sells your data. We're done." Render 4 VillainCards in a responsive grid (2x2 on desktop, single column on mobile). Each card staggers its entrance animation using GSAP ScrollTrigger. Use `useGSAP` hook from `@gsap/react` for cleanup. Register ScrollTrigger plugin. Add "use client" directive.

**RevenueComparison `src/components/sections/RevenueComparison.tsx`:**
Section heading: "WHERE YOUR MONEY ACTUALLY GOES" or "THE MONEY THEY STEAL". Show 3 horizontal animated bars:
- ULTRASTREAM: 95-100% (creator keeps) -- brand accent color, glowing
- YouTube: 70% -- muted red
- Twitch: 50% -- muted purple
Each bar animates in sequence via GSAP ScrollTrigger timeline. Below the bars, add a callout stat: "If you make $10,000/month streaming: On Twitch you keep $5,000. On ULTRASTREAM you keep $9,500-$10,000." Use GSAP for number counter animation on the dollar amounts. Add "use client" directive.

**Tone throughout:** Anti-corporate. "Built for the people." This isn't a polite comparison chart -- it's a call to arms. Copy should make creators angry at the status quo and excited about the alternative.

**DO NOT:**
- Import actual trademarked logos (use stylized text or abstract representations)
- Use crypto jargon ("smart contracts" becomes "automatic payments", "decentralized" becomes "community-powered" or "no single authority")
- Make it feel like a generic SaaS comparison page -- this is a rebellion manifesto
  </action>
  <verify>
Run `npx tsc --noEmit` -- no TypeScript errors. Visually verify in dev server (`npm run dev`) that the Villains section renders 4 cards in a grid, each with pain point text and solution text. Revenue comparison shows 3 animated bars. Scroll down the page -- cards and bars should animate in via ScrollTrigger.
  </verify>
  <done>
4 villain cards render with Twitch/YouTube/TikTok/Discord pain points and ULTRASTREAM solutions. Revenue comparison shows animated bars for ULTRASTREAM (95-100%), YouTube (70%), Twitch (50%). All animations trigger on scroll. Anti-corporate tone is consistent. No trademarked assets used. Covers PAIN-01, PAIN-02, PAIN-03, PAIN-04.
  </done>
</task>

<task type="auto">
  <name>Task 2: Feature showcase sections and ULTRAVERSE connection</name>
  <files>
    src/lib/content/features.ts
    src/components/ui/FeatureCard.tsx
    src/components/sections/FeaturesShowcase.tsx
    src/components/sections/UltraverseSection.tsx
    src/app/page.tsx
  </files>
  <action>
Build the 6 feature showcase sections and the ULTRAVERSE ecosystem section, then wire everything into the main page.

**Content data file `src/lib/content/features.ts`:**
Export a `features` array of 6 objects, each with:
- `id`: string identifier
- `title`: Human-language feature name (NOT crypto jargon)
- `tagline`: One-line rally cry
- `description`: 2-3 sentences explaining what this means for creators/viewers in plain language
- `icon`: Icon identifier or emoji placeholder
- `accentColor`: Unique accent per feature

The 6 features with their human-language framing:

1. **Creator Monetization** (FEAT-01)
   - Title: "YOUR MONEY STAYS YOURS"
   - Tagline: "0-5% platform fee. Not 50%. Not 30%. Yours."
   - Description: Smart subscriptions and peer-to-peer tipping mean your supporters pay YOU, not a corporation. Automatic payments flow directly to your wallet. No waiting 30 days for a check. No mysterious deductions.

2. **Viewer Rewards** (FEAT-02)
   - Title: "GET PAID TO WATCH"
   - Tagline: "Your time has value. We actually pay you for it."
   - Description: Proof-of-Engagement rewards you with KZR tokens for actually watching and participating. Share your bandwidth, earn more. This isn't fake channel points -- it's real value you can spend or save.

3. **Discovery Engine** (FEAT-03)
   - Title: "YOUR CONTENT WILL BE SEEN"
   - Tagline: "New creators get boosted, not buried."
   - Description: Our discovery system uses community engagement signals -- not corporate algorithms -- to surface content. New creators get an automatic boost. Your audience finds you because the community vouches for you, not because you gamed an algorithm.

4. **Censorship Resistance** (FEAT-04)
   - Title: "NO ONE CAN SILENCE YOU"
   - Tagline: "Community-powered delivery. No single kill switch."
   - Description: Content flows through a community mesh network and lives on distributed storage. Rules are set by The Architects -- a community council, not a corporate boardroom. No single authority can pull the plug on your career overnight.

5. **Privacy Age Verification** (FEAT-05)
   - Title: "VERIFY WITHOUT SURRENDERING YOUR IDENTITY"
   - Tagline: "We confirm your age, then forget everything."
   - Description: Other platforms store your government ID and get breached. We verify your age and scramble everything private. Nothing stored. Nothing to hack. Nothing to sell. Compliant with the latest FTC guidance -- because privacy isn't optional.

6. **ULTRAVERSE Ecosystem** (FEAT-06)
   - Title: "PART OF SOMETHING BIGGER"
   - Tagline: "ULTRASTREAM + ULTRAVERSE.games = the future of gaming entertainment."
   - Description: ULTRASTREAM lives within the ULTRAVERSE.games ecosystem. Cross-platform benefits, shared community, unified token economy. Your streaming career connects to the entire gaming universe.

**FeatureCard component `src/components/ui/FeatureCard.tsx`:**
A feature presentation card. Props: feature data object + index (for stagger). Each card has: icon area (top), title, tagline (bold, accent-colored), description, and a subtle accent border/glow matching the feature's color. Cards animate in via GSAP ScrollTrigger -- slide up with fade, staggered by index. On hover, the accent glow intensifies (CSS transition, not GSAP -- keep it lightweight). Responsive: full width on mobile, 2-column or 3-column grid on desktop.

**FeaturesShowcase `src/components/sections/FeaturesShowcase.tsx`:**
Section heading: "THE WEAPONS" or "OUR ARSENAL" -- continuing the rebellion metaphor. Subheading: "Six ways we're building the platform creators and viewers deserve." Renders all 6 FeatureCards. Layout: On large screens, use a 2-column grid with alternating visual emphasis (odd features get a slightly different layout than even -- keeps visual interest). On mobile, single column. GSAP ScrollTrigger batch animation -- cards reveal as user scrolls through. Each feature section should feel like its own moment, not a wall of text. Add generous vertical spacing between features. Use `useGSAP` hook. Add "use client" directive.

**UltraverseSection `src/components/sections/UltraverseSection.tsx`:**
A dedicated but subtle ecosystem section. Heading: "BORN FROM ULTRAVERSE.GAMES". Explain the parent relationship without overshadowing ULTRASTREAM's identity. Show: ULTRAVERSE.games logo/text, brief explanation of cross-platform benefits (shared token economy, unified gaming community, cross-platform discovery). Visual: Use a connecting line or network visualization motif. GSAP fade-in on scroll. Keep it concise -- 1-2 paragraphs max. This bridges to FEAT-06 but gives the ecosystem its own real estate. Add "use client" directive.

**Wire into page `src/app/page.tsx`:**
Import and render all new sections into the main page in order:
1. (Hero -- already exists from Phase 2)
2. VillainsSection (The Problem)
3. RevenueComparison (The Money)
4. FeaturesShowcase (The Weapons)
5. UltraverseSection (The Ecosystem)
6. (Footer area -- waitlist/CTA added in Plan 03-02)

Each section should have an `id` attribute for navigation anchoring (id="villains", id="revenue", id="features", id="ultraverse"). Ensure smooth scroll behavior between sections works with the Lenis smooth scroll from Phase 2.

**Tone:** Every heading should feel like a battle cry. Descriptions are human, warm, and specific. The rebellion metaphor (villains/weapons/arsenal) runs throughout without becoming cheesy -- the specificity of the pain points keeps it grounded.

**DO NOT:**
- Use words like "blockchain", "smart contract", "decentralized", "DAO" in user-facing copy (use human equivalents)
- Create a boring feature grid that looks like every other SaaS site
- Forget mobile responsiveness -- gaming audience is heavily mobile
- Skip GSAP cleanup (always use useGSAP or manual cleanup in useEffect return)
  </action>
  <verify>
Run `npx tsc --noEmit` -- no TypeScript errors. Run `npm run dev` and scroll through the full page. Verify: 6 feature cards render with correct titles and descriptions, ULTRAVERSE section renders, all sections animate on scroll via GSAP ScrollTrigger, page section order is correct (Villains > Revenue > Features > ULTRAVERSE), sections have id attributes for nav anchoring, layout is responsive (check at 375px and 1440px viewport widths).
  </verify>
  <done>
6 feature showcase sections render with human-language descriptions, scroll-triggered GSAP animations, and rebellion-themed headings. ULTRAVERSE ecosystem section positions ULTRASTREAM within parent platform. All sections wired into page.tsx with anchor IDs. Responsive grid layout works on mobile and desktop. Covers FEAT-01, FEAT-02, FEAT-03, FEAT-04, FEAT-05, FEAT-06.
  </done>
</task>

</tasks>

<verification>
- All 14 Phase 3 content requirements (PAIN-01..04, FEAT-01..06) addressed by this plan's sections
- TypeScript compiles without errors
- All GSAP animations use proper cleanup (useGSAP hook or manual cleanup)
- No crypto jargon in user-facing copy
- No trademarked logos imported
- Responsive at mobile (375px) and desktop (1440px)
- Section anchor IDs present for navigation
</verification>

<success_criteria>
1. "The Problem" section viscerally communicates how Twitch/YouTube/TikTok/Discord screw creators with specific data points from research
2. Revenue comparison visual makes the 0-5% vs 50% vs 30% difference immediately obvious through animated bars
3. Each of the 4 pain points pairs with ULTRASTREAM's solution (villain killed by weapon)
4. All 6 platform features have dedicated showcase sections with human-language explanations and GSAP scroll animations
5. ULTRAVERSE.games ecosystem section exists and connects without overshadowing
6. Anti-corporate rebellion tone is consistent throughout all sections
</success_criteria>

<output>
After completion, create `.planning/phases/03-content-waitlist/03-01-SUMMARY.md`
</output>
